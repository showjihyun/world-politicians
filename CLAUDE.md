# POLARIS — 작업 규칙

미국 정치인 관계 그래프. React + TS 앱, 야간 뉴스 파이프라인, 데이터는 저장소 안에.
설계 근거는 `docs/adr/`, 극성 집계와 언론 편향 판단의 근거는 `docs/research/`.
여기에는 **모르고 하면 사고가 나는 것**만 적는다.

## 명령

```
npm test           단위 테스트 (도메인, ~0.4초)
npm run audit      경계 + 데이터 감사 (종료 코드로 판정)
npm run e2e        Playwright 85개 (~90초, dev 서버 자동 기동)
npm run news:dry   뉴스 파이프라인 미리보기 (실제 수집은 야간 자동)
npm run news:recover:dry  지워진 판정을 git 이력에서 되살리기 미리보기
npm run crosswalk:dry  소스 간 ID 크로스워크 미리보기 (bioguide·icpsr·fec)
npm run cosponsor:dry  공동발의 엣지 미리보기 (GovInfo 벌크)
npm run funding:dry    FEC 자금 미리보기 (기부·독립지출)
npm run lobbying:dry   로비 회전문 미리보기 (하원 LD-1 원본)
npm run party-unity:dry 당론 이탈률 미리보기 (Voteview 호명투표)
npm run eval           LLM 판정 정확도 (라벨 세트 채점)
npm run eval:sample    라벨 표본 추가 (--dry 지원)
npm run build      tsc + vite
```

작업을 끝냈다고 말하기 전에 최소한 `npm test && npm run audit` 은 돌린다.
데이터나 파이프라인을 건드렸으면 `npm run e2e` 까지.

## 이 저장소에서 실제로 사고를 낸 것들

### 줄바꿈이 섞여 있다

한 저장소 안에 CRLF 파일과 LF 파일이 섞여 있다(현재 대략 16:51).
어느 쪽인지 파일마다 다르므로 가정할 수 없다. 스크립트로 텍스트를 치환할 때 `\n` 기준으로만 매칭하면
CRLF 파일에서 **조용히 실패**한다(에러가 아니라 "못 찾음"으로 끝난다).
읽을 때 `\r\n` → `\n` 으로 정규화하고, 쓸 때 원래 형태로 되돌린다.

### 통과는 아무것도 증명하지 않는다

이 저장소에서 "확인했다" 가 거짓이었던 사례가 반복됐다.

- 링크 생존 확인 20/20 통과 → 실제로는 `news.google.com` 이 자기 페이지에
  200 을 준 것이었고 기사 존재 여부와 무관했다
- 감사 스크립트가 통과인데 종료 코드 127 → `process.exit()` 이 Windows 에서
  이벤트 루프를 끊어 코드를 뭉갰다. **일이 끝난 뒤에는 `process.exitCode`**
  (I/O 가 떠 있는데 `exit()` 하면 코드가 뭉개진다). 반대로 **맨 앞의 가드에서는
  `exit()` 가 맞다** — 아직 아무것도 시작하지 않았고, 멈추지 않으면 키 없이
  호출을 계속한다. 이 둘을 뒤집어 가드를 `exitCode` 로 바꿨다가 "중단" 을 찍고
  그대로 진행하게 만든 적이 있다
- E2E 가 "잘 돈다" → 고정 대기가 framer-motion 퇴장 애니메이션과 레이스여서
  간헐 실패였다. 애니메이션은 `waitFor({ state: 'detached' })` 로 기다린다
- `docs.claims` 가 계속 통과 → **정규식이 아무것도 매칭하지 않았다.** `checkDocClaims`
  는 못 찾으면 `continue` 한다. 그 사이 README 문장이 바뀌어 패턴에서 벗어났고,
  검사는 "맞다" 가 아니라 **"볼 게 없다"** 를 통과로 보고하고 있었다. `README.ko.md`
  는 살아 있는 검사가 아예 0개였다. 이런 검사는 **없는 것보다 나쁘다** — 있다고
  믿게 만든다. 문서 수치 검사를 건드릴 때는 숫자를 틀리게 바꿔 경고가 뜨는지 본다.
  참고로 `docs.claims` 는 WARN 이라 종료 코드를 막지 않는다

**검사를 만들면 실패도 시켜 본다.** 불량을 주입해 종료 코드 1 이 나오는지
확인하지 않은 검사는 검사가 아니다. **매칭이 0건인 것과 통과는 다르다.**

### LLM 판정에는 정답지가 있어야 한다

신호 299건이 전부 LLM 판정(극성·관계쌍)을 달고 화면에 나가는데, 얼마나 맞는지
재는 장치가 없었다. 없으면 프롬프트를 고치고 **"좋아졌다" 고 믿게 된다** — 이
저장소에서 "확인했다" 가 거짓이었던 사례가 반복됐다.

`scripts/eval/labels.json` 이 정답지다. `npm run eval:sample` 로 층화 표본을 뽑고
(극성 × 허브 여부 — feud 146건 중 108건이 trump 관련이라 나누지 않으면 표본이
통째로 쏠린다), 사람이 `truth` 를 채우고, `npm run eval` 이 채점한다.

지켜야 하는 것 셋.

- **표본은 시드로 고정한다.** 매번 다른 기사를 뽑으면 프롬프트 효과와 표본 운을
  구분할 수 없다. 시드를 바꾸면 라벨을 버리게 된다
- **라벨은 스냅샷을 들고 있는다.** 신호는 365일 뒤 아카이브에서 빠진다. id 만
  들고 있으면 그때 라벨이 통째로 죽는다
- **모델이 라벨을 채우지 않는다.** 파이프라인을 같은 종류의 판단으로 채점하면
  점수가 거짓으로 높아진다. 사람이 채운 칸만 센다
- **오타를 오답으로 세지 않는다.** `Feud`·`feud ` 는 봐주고, `conflict` 처럼 목록에
  없는 값은 채점에서 빼고 감사가 잡는다. 조용히 오답으로 세면 모델이 맞았는데
  점수가 떨어지고, 그 상태로 잡은 기준선은 낮게 박혀 **진짜 하락을 영영 못 잡는다**

감사는 절대 점수로 막지 않는다. 기준선을 한 번 적어 두면 **그 아래로 떨어질 때만**
잡는다(허용 -3%p).

### 외부 응답 파싱을 복제하지 않는다

`extractJsonArray` 가 네 곳에 복제돼 있었다. 지금은 동작이 같지만 이미 이름과
서식이 갈라져 있었다. 이게 조용히 틀리면 **빈 배열**이 나오고, 파이프라인은 빈
배열을 "판정 없음" 으로 처리해 원본을 유지하므로 **에러도 로그도 없이 그날 배치가
아무 일도 안 한 것**이 된다.

`parse-core.mts` 하나로 모으고 테스트를 붙였다. `ask`(LLM 호출·재시도)도 세 벌이라
`llm.mts` 로 모았다 — 재시도 횟수가 갈라지면 한 스크립트만 일찍 포기한다.

### 조인 키를 복제하지 않는다

`pairKey` 가 아홉 군데에 복제돼 있었다. 이 키는 `relationship-sources.json`·
`cosponsorship-sources.json`·그래프를 잇는 조인 키라서, 하나만 달라지면
**에러 없이 근거 패널이 비어서 나온다.** `strengthOf` 가 두 벌이라 테스트한 쪽이
안 돌던 것과 같은 종류다.

정본은 두 개다 — 앱은 `src/domain/graph.ts`, 스크립트는 `scripts/sources/keys-core.mts`.
빌드 문맥이 달라 한 파일을 공유할 수 없다. `checks.mts` 는 순수성 때문에 값 import 가
금지돼 자기 것을 쓴다. **셋이 같은 값을 내는지는 `keys-core.test.mts` 의 계약
테스트가 고정한다.**

### 이름을 성으로 맞추지 않는다

로비 신고서의 `coveredPosition` 은 "Legislative Director, Rep. Marsha Blackburn"
같은 자유 문자열이다. 여기서 의원을 뽑을 때 **성만 쓰면 절반이 거짓**이다 —
역대 의원 중 Harris 가 33명, Graham 이 14명이다. `Rep./Sen. + 전체 이름` 으로
좁히면 역대 12,768명 기준으로도 동명이인이 0건이었다.

**왜 그 사람에게 붙었는지 화면에 남긴다.** 근거가 될 문구를 잘라 보여주는데,
앞에서부터 120자로 자르면 `…Republican Leader Mitch …` 처럼 이름이 잘려 나간다.
이름을 중심으로 창을 잡고, 같은 사람이 호칭 없이도 나올 때는 호칭 붙은 조각을
고른다. `audit:data` 의 `lobbying.evidence` 가 이걸 검사한다 — 처음 만들었을 때
14건이 걸렸다.

### 경로를 지어내고 그 에러로 사이트를 판정하지 않는다

`disclosurespreview.house.gov` 를 "죽었다" 고 세 번 적었다. 실제로는 루트가 계속
200 이었고, 내가 지어낸 경로(`/data/LD/2026/2026_1.zip`, `/lda.aspx`)가 500 을 준
것이었다. 랜딩 페이지를 읽었으면 첫 시도에 끝났다 — 페이지에 **"이 사이트는
7월 31일부터 ZIP 다운로드를 제외하고 사용 불가"** 라고 적혀 있었고, 파일 목록은
`data/LD/LdSearchPastFilings.json` 이 준다.

그 결과 로비 데이터가 없다고 판단해 3자 미러를 쓰려 했다. 1차 소스는 2002년부터
113개 아카이브가 키 없이 열려 있었다.

**HTML 에서 URL 을 주워 소스라고 부르지도 않는다.** `lda.congress.gov` 를 후속
데이터 소스로 적었는데, 주변 텍스트를 읽어 보니 로비스트가 신고서를 **제출**하는
포털이었다(계정 필요, 안내문은 2015년 것). 403 도 근거가 못 됐다 —
`www.congress.gov` 가 curl 에 똑같은 403 을 준다. Cloudflare 가 도메인 전체에 걸려
있을 뿐이라 그 호스트에 대해 아무것도 말해주지 않는다.

### 돈 데이터는 합치면 뜻이 뒤집힌다

FEC 의 `pas2` 에는 세 가지가 한 파일에 섞여 있다. **거래 유형으로 갈라야 한다.**

```
24K  후보에게 직접 기부          $32.0M
24E  후보를 지지하는 독립지출     $14.4M   ← 후보와 조율 금지, 기부 아님
24A  후보를 반대하는 독립지출     $18.1M   ← 공격이다. 합치면 후원으로 보인다
```

반대 지출이 지지보다 크다. 유형을 안 보고 합치면 Massie 를 공격한 $10.1M 이
그의 후원금이 된다.

**공동모금위원회(`CMTE_DSGN='J'`)에서 온 돈은 본인 돈이다.** 안 빼면 Mike Johnson 의
1위 후원자가 본인 공동모금위원회($100만)로 나온다. PAC 의 후보 기부 한도는 주기당
$10,000 이므로 그 금액 자체가 기부일 수 없다는 신호였다.

**환불은 음수로 기록된다.** 24K 의 6,187건이 음수다(-$14.9M). 은퇴·사임한 인물은
PAC 순액이 음수가 되는데 파싱 오류가 아니라 "돈을 돌려줬다" 는 사실이다.
음수를 오류로 취급하는 검사를 먼저 만들었다가 9명이 걸려서 전제를 고쳤다.

그리고 이 레이어가 설명하는 돈은 **전체 수입의 6%** 다. 나머지 73% 는 개인 기부인데
누가 냈는지는 별도의 거대한 파일에 있고 200달러 초과분만 항목화된다. 화면에 비중과
단서를 함께 적는 이유다.

### 편집 판단과 측정값을 같은 선으로 그리지 않는다

엣지에는 두 종류가 있다. 큐레이션한 266개는 "동맹이다" 라는 **편집 판단**이고,
공동발의 125개는 "119대에서 78건을 함께 발의했다" 는 **측정값**이다. 같은 선으로
그리면 보는 사람이 둘을 구분할 수 없고, 그러면 검증 가능한 쪽까지 신뢰를 잃는다.
그래서 `cosponsor` 는 별도 타입이고 범례에서 따로 끈다.

**공동발의는 방향이 있다.** B 가 A 의 법안에 서명한 것이지 그 반대가 아니다. 쌍으로
합칠 때 방향을 버리면 15/0(일방적 지지)과 15/15(상호)가 같은 선이 된다 — 125쌍 중
25쌍이 80% 이상 일방적이다. 65% 이상 기울면 `initiator` 에 **서명한 쪽**을 넣고,
그 미만은 상호적이라고 보고 방향을 붙이지 않는다.

`initiator` 는 원래 feud 전용이었는데 도메인에서 일반화했다. 뜻은 둘 다 "여기서
무언가가 상대에게로 간다" 이다. a·b 를 id 순으로 뒤집을 때 방향 집계를 같이 뒤집지
않으면 **화살표가 정확히 반대**를 가리키는데 눈으로는 그럴듯해서 못 잡는다.
`audit:data` 의 `cosponsor.direction` 이 이걸 본다.

**"같은 당" 은 정당이 아니라 코커스로 본다.** Sanders·King 은 무소속이지만 민주당과
코커스한다. 정당 문자열만 비교하면 Markey × Sanders 가 "초당적" 이 되어 초당적 쌍이
11개에서 19개로 부푼다. 크로스워크의 `caucus` 필드를 쓴다.

원시 건수를 그대로 쓰면 안 되는 이유도 있다. 10건 이상 쌍의 84% 가 같은 당이다.
표결 일치도(Cruz × Hawley 98.6%)가 관계가 아니라 소속이었던 것과 같은 함정이다.
기준선을 10건에 둔 것은 5건이면 신규 264개가 되어 큐레이션한 266개가 묻히기 때문이다.

이미 큐레이션된 쌍은 `duplicate` 로 표시해 그래프에서 뺀다 — 두 노드 사이에 선을
두 번 그으면 읽을 수 없다. `audit:data` 가 이 표시가 실제와 맞는지 검사한다.

### 분류 실패는 되돌아오지 않는다

파이프라인은 **새로 수집한 기사만** 분류한다. 그래서 한 번 실패한 신호는 다시 보지
않고 영원히 미분류로 남는다. 실제로 2026-08-12~17 엿새치 29건(9.8%)이 갇혀 있었다 —
기사가 어려워서가 아니라 그 기간 분류가 실패한 것이다. 감사에도 로그에도 화면에도
흔적이 없었다.

세 가지를 함께 둔다.

- `reclassify` 가 합쳐진 신호에서 갇힌 미분류를 다시 시도한다(한 번에
  `llm.retryLimit` 건까지). 또 실패하면 원본을 그대로 둔다 — 침묵을 덮어쓰기로
  처리하면 이미 분류된 것까지 지워진다
- 화면에는 `미판정` 이라고 적는다. 아무 표시 없이 내보내면 "판정이 없다" 가 아니라
  그냥 빈약한 항목으로 보인다. 근거 없는 엣지를 "근거 없음" 이라고 적는 것과 같다
- `audit:data` 의 `signals.unclassified` 가 비율이 5% 를 넘으면 경고한다.
  실패로 막지는 않는다 — LLM 이 잠깐 죽는 것은 데이터 오류가 아니다

`bySalience` 가 미분류를 뒤로 밀기 때문에 화면에 실제로 보이는 것은 적다(65명 중
4명, 8건). 그래도 라벨을 붙이는 이유는 그 8건이 **판정을 못 받았다는 사실**이기 때문이다.

### 신호 id 는 제목을 품고 있다

신호 id 는 `hash(url + title)` 이다. 매체가 헤드라인을 고치거나 영상판에 `Video `
접두사가 붙으면 **같은 기사가 다른 id 로 다시 쌓인다.** `accumulate` 는 id 로만
거르므로 통과한다. 실제로 두 건이 그렇게 들어왔고, 그중 하나는 미분류라 화면에
극성 없이 나갔다.

id 체계는 바꾸지 않는다 — 바꾸면 쌓인 신호의 id 가 전부 달라져 다음 실행이
아카이브를 두 배로 만든다. 대신 `dedupeByStory` 가 **(url, 관계쌍)** 으로 한 번 더
거르고, `audit:data` 의 `signals.duplicate` 가 남은 것을 잡는다.

재처리로 아카이브를 고칠 때는 `finalize.mts` 를 쓴다. **`fresh` 를 주지 않는다** —
수집이 아니라 재처리이므로 수집 시각이 갱신되면 안 된다.

### 매칭 실패를 확정으로 취급하지 않는다

크로스워크에서 "후보를 못 찾음" 을 자동 확정으로 처리했더니 Mike Lawler·Dick Durbin·
Chris Murphy 등 **7명이 조용히 빠졌다.** 데이터에는 Michael·Richard·Christopher 로
들어있고 별칭 필드는 비어 있다. 실패가 아니라 "해당 없음" 으로 끝나서 아무도 몰랐다.

통칭 사전(Mike→Michael)을 코드에 넣는 것은 답이 아니다 — 그 사전이 곧 새로운
흔들림의 원인이 된다. **성이 같은데 못 맞춘 경우를 사람에게 올리고**, 확정값을
`crosswalk-overrides.json` 에 적는다. 적지 않으면 쓰지 않고 종료 코드 1 로 끝난다.
같은 이유로 `Number('')` 는 `0` 이다 — 빈 icpsr 를 그냥 넘기면 0번 의원이 생긴다.

### 파괴적 스크립트는 `--dry` 먼저

`scripts/sources/*` 와 파이프라인은 데이터를 덮어쓴다. 제거율을 먼저 보고
반영한다. 실제로 필터가 802 → 381(52% 제거)이었고, 이 수치를 보지 않고
반영했으면 되돌리기 어려웠다.

플래그 방향은 하나다. **`--dry` = 쓰지 않는다.** 부작용을 "켜는" 플래그를
새로 만들지 않는다 — 어느 쪽이 안전한지 매번 확인하게 된다.
`--fetch`(네트워크 단계)와 `--links`(느린 링크 검사)는 쓰기와 다른 축이라 예외로
등록돼 있다. 새 플래그가 필요하면 `scripts/audit/conventions.mts` 의 목록에 먼저
추가한다. `audit:conventions` 가 이 관례를 강제한다.

### LLM 배치에서 침묵을 삭제로 해석하지 않는다

판정을 못 받은 항목은 **원본을 유지**한다. dry-run 중 503 이 2회 났고,
침묵을 "제거" 로 처리했다면 그 배치의 근거가 통째로 날아갔다.

**이 원칙은 분류 단계에만 있고 병합 단계에는 없었다.** `applyResult` 는 지켰는데
`accumulate` 가 incoming 을 id 로 무조건 `map.set` 했다. 30일 창 안의 기사는 매일
다시 수집되므로, 그날 LLM 이 죽으면 미분류 신규가 **이미 분류된 아카이브를 덮는다.**
2026-09-01 수집이 그렇게 25건의 판정을 지웠다 — 에러도 로그도 없었고, 감사에는
"미분류 9.2%" 라는 결과만 남아 기사가 어려운 것처럼 보였다. 지금은 incoming 이
미분류이고 existing 이 분류돼 있으면 existing 을 지킨다.

교훈은 **"판정을 유지한다" 를 판정하는 곳에만 적어 두면 안 된다** 는 것이다.
데이터가 지나가는 모든 합류 지점에 같은 규칙이 필요하다. 지워진 판정은
`npm run news:recover` 가 git 이력에서 되살린다(`--dry` 먼저, `fresh` 없이).
복구로 쌍이 돌아오면 **중복 판정 키가 바뀌어 새 중복이 드러난다** — 실제로 2건이
그랬다. 데이터를 고치는 변경 뒤에는 감사를 다시 돌린다.

### `generatedAt` 은 재처리가 갱신하면 안 된다

"뉴스를 수집한 시각" 이지 "파일을 다시 쓴 시각" 이 아니다. 분할·정규화가
이 값을 덮어써서 화면 배지가 27.7시간 어긋난 적이 있다.
`writeOutput` 의 기본은 보존이고, 실제 수집만 `{ fresh: true }` 를 준다.

### 출력 구조를 바꾸면 워크플로가 커밋하는 경로도 바꾼다

월별 분할로 `news-signals.json` 을 지웠는데 `daily-news.yml` 이 그 경로를
계속 스테이징하고 있었다. `git add` 는 없는 경로에서 실패하므로, 수집은
성공하고 **커밋 단계에서 죽어 그날 누적분이 통째로 유실될** 뻔했다.

### 인물 속성과 관계 신호를 섞지 않는다

자금·로비·당론 이탈률은 **인물에 대한 사실**이지 관계에 대한 값이 아니다.
그럴듯해 보여서 엣지로 만들고 싶어지는데, 이 저장소에서 여섯 번 재서 여섯 번 다
무신호였다.

```
공유 후원자 × 큐레이션 ally/feud    27% vs 28%
공유 후원자 × 공동발의 20건 이상    19% vs 17%
PAC 비중   × 초당파 엣지            10.3% vs 8.3%
NOMINATE 이탈 × 당내 feud           p = 0.813
당론 이탈률 × 당내 feud             p = 0.992
당론 이탈률 × 외부 공격 자금        p = 0.058 (용량-반응 없음, 경쟁도 교란)
```

Collins 16.5% · Fitzpatrick 22.9% 는 이탈률 상위인데 당내 feud 가 0 이다 —
**조용히 상시 이탈하는 것은 공개 충돌을 낳지 않는다.** 이 그래프의 feud 는
시끄럽고 인격적인 충돌이고 표결 축과 직교한다.

표결 **일치도**는 이미 기각돼 있다(Cruz × Hawley 98.6% = 소속). 이탈률은 자기
당을 기준으로 잡아 소속을 상수로 만들기 때문에 다른 값이고, 그래서 인물 사실로는
쓸 만하다. 다만 그 이상을 주장하면 안 되고, 화면 단서에 이 값이 무엇이 **아닌지**
를 적어 둔다.

순위표도 조심한다. "가장 공격받은 인물" 은 순위가 아니었다 — 반대지출 총액의
56% 가 한 명이고 89% 가 셋이다. 회전문 인원수는 상원 5.6명 대 하원 2.9명으로
**보좌진 규모 × 재직 연수**를 센다.

### 근거 링크는 확인 가능한 것만

Google News 리다이렉트(`news.google.com/rss/articles/...`)는 목적지도 매체도
확인할 수 없다. 근거 패널에 넣지 않는다 — `audit:data` 가 막는다.
뉴스 신호(Latest Wire)는 별개다. 거기서는 "최근 이런 보도가 있었다" 용도라
기준이 다르다.

## 구조

```
src/domain/                      순수. 값 import 금지 (audit:boundary 가 강제)
scripts/news-pipeline/core.mts   누적·선별·파티션 규칙 (순수)
scripts/audit/checks.mts         감사 판정 규칙 (순수)
scripts/sources/*-core.mts       소스별 판정 규칙 (순수). keys-core 만 import 허용
scripts/sources/keys-core.mts    조인 키·이름 정규화 정본 (스크립트 쪽)
scripts/sources/parse-core.mts   외부 텍스트 파싱 정본 (LLM 응답·RSS)
scripts/sources/llm.mts          LLM 호출 어댑터 (재시도·빈 배열 반환)
scripts/eval/labels.json         정확도 라벨 세트 (사람이 truth 를 채운다)

src/lib/, scripts/**/*.mts       어댑터. 데이터셋·fs·network 를 여기서 묶는다
src/lib/lazy-dataset.ts          인물 상세에서 늦게 불러오는 데이터셋 공통 (자금·로비·이탈률)
src/data/signals/                월별 파티션 + 매니페스트 + recent
src/data/crosswalk.json          소스 간 ID 확정값 (bioguide·icpsr·fec)
src/data/cosponsorship*.json     공동발의 엣지 + 근거 (엣지는 즉시, 근거는 지연)
src/data/funding.json            FEC 자금 (지연 로딩, 인물 속성)
src/data/lobbying.json           로비 회전문 (지연 로딩, 인물 속성)
src/data/party-unity.json        당론 이탈률 (지연 로딩, 인물 속성). 관계 신호가 아니다
```

도메인에 값 import 를 넣고 싶어지면 그건 인자로 받아야 할 것이다.
이유는 ADR 0001.

## 데이터

- 저장소가 곧 DB 다. 야간 커밋 하나하나가 스냅샷이고, 실제로 데이터가 잘렸을 때
  git 이력으로 복구했다. DB 를 넣을 조건은 ADR 0002 에 적어 뒀다
- 아카이브는 365일 누적. **덮어쓰지 않는다** — 신규가 비어도 기존은 남아야 한다
- 문서(README)의 수치는 손으로 갱신해 왔고 조용히 낡는다. `audit:data` 가
  실제 데이터와 대조한다

## 하네스: POLARIS

**목표:** 관계 그래프·수집 파이프라인·근거·화면·문서에 걸친 작업을 영역별
전문 에이전트로 나누고, 각 단계를 실행 가능한 검증으로 판정한다.

**트리거:** 여러 영역에 걸치거나 범위가 넓은 요청은 `polaris` 스킬을 사용하라.
한 파일 수정이나 단순 질문은 직접 처리해도 된다.

에이전트는 `.claude/agents/`, 스킬은 `.claude/skills/` 에 있다.
구축 전에 무엇이 필요했는지와 그 근거는 ADR 0003.

**변경 이력:**

| 날짜 | 변경 내용 | 대상 | 사유 |
| --- | --- | --- | --- |
| 2026-08-29 | 초기 구성 (에이전트 5, 스킬 3) | 전체 | ADR 0003 의 선행 조건 1~4 완료 후 |
