# POLARIS — 작업 규칙

미국 정치인 관계 그래프. React + TS 앱, 야간 뉴스 파이프라인, 데이터는 저장소 안에.
설계 근거는 `docs/adr/`. 여기에는 **모르고 하면 사고가 나는 것**만 적는다.

## 명령

```
npm test           단위 테스트 (도메인, ~0.4초)
npm run audit      경계 + 데이터 감사 (종료 코드로 판정)
npm run e2e        Playwright 53개 (~90초, dev 서버 자동 기동)
npm run news:dry   뉴스 파이프라인 미리보기 (실제 수집은 야간 자동)
npm run crosswalk:dry  소스 간 ID 크로스워크 미리보기 (bioguide·icpsr·fec)
npm run cosponsor:dry  공동발의 엣지 미리보기 (GovInfo 벌크)
npm run funding:dry    FEC 자금 미리보기 (기부·독립지출)
npm run build      tsc + vite
```

작업을 끝냈다고 말하기 전에 최소한 `npm test && npm run audit` 은 돌린다.
데이터나 파이프라인을 건드렸으면 `npm run e2e` 까지.

## 이 저장소에서 실제로 사고를 낸 것들

### 줄바꿈이 섞여 있다

한 저장소 안에 CRLF 파일과 LF 파일이 섞여 있다(현재 대략 16:51).
어느 쪽인지 파일마다 다르므로 가정할 수 없다. 스크립트로 텍스트를 치환할 때 `\n` 기준으로만 매칭하면
CRLF 파일에서 **조용히 실패**한다(에러가 아니라 "못 찾음"으로 끝난다).
읽을 때 `\r\n` → `\n` 으로 정규화하고, 쓸 때 원래 형태로 되돌린다.

### 통과는 아무것도 증명하지 않는다

이 저장소에서 "확인했다" 가 거짓이었던 사례가 반복됐다.

- 링크 생존 확인 20/20 통과 → 실제로는 `news.google.com` 이 자기 페이지에
  200 을 준 것이었고 기사 존재 여부와 무관했다
- 감사 스크립트가 통과인데 종료 코드 127 → `process.exit()` 이 Windows 에서
  이벤트 루프를 끊어 코드를 뭉갰다. **`process.exit()` 대신 `process.exitCode`**
- E2E 가 "잘 돈다" → 고정 대기가 framer-motion 퇴장 애니메이션과 레이스여서
  간헐 실패였다. 애니메이션은 `waitFor({ state: 'detached' })` 로 기다린다

**검사를 만들면 실패도 시켜 본다.** 불량을 주입해 종료 코드 1 이 나오는지
확인하지 않은 검사는 검사가 아니다.

### 돈 데이터는 합치면 뜻이 뒤집힌다

FEC 의 `pas2` 에는 세 가지가 한 파일에 섞여 있다. **거래 유형으로 갈라야 한다.**

```
24K  후보에게 직접 기부          $32.0M
24E  후보를 지지하는 독립지출     $14.4M   ← 후보와 조율 금지, 기부 아님
24A  후보를 반대하는 독립지출     $18.1M   ← 공격이다. 합치면 후원으로 보인다
```

반대 지출이 지지보다 크다. 유형을 안 보고 합치면 Massie 를 공격한 $10.1M 이
그의 후원금이 된다.

**공동모금위원회(`CMTE_DSGN='J'`)에서 온 돈은 본인 돈이다.** 안 빼면 Mike Johnson 의
1위 후원자가 본인 공동모금위원회($100만)로 나온다. PAC 의 후보 기부 한도는 주기당
$10,000 이므로 그 금액 자체가 기부일 수 없다는 신호였다.

**환불은 음수로 기록된다.** 24K 의 6,187건이 음수다(-$14.9M). 은퇴·사임한 인물은
PAC 순액이 음수가 되는데 파싱 오류가 아니라 "돈을 돌려줬다" 는 사실이다.
음수를 오류로 취급하는 검사를 먼저 만들었다가 9명이 걸려서 전제를 고쳤다.

그리고 이 레이어가 설명하는 돈은 **전체 수입의 6%** 다. 나머지 73% 는 개인 기부인데
누가 냈는지는 별도의 거대한 파일에 있고 200달러 초과분만 항목화된다. 화면에 비중과
단서를 함께 적는 이유다.

### 편집 판단과 측정값을 같은 선으로 그리지 않는다

엣지에는 두 종류가 있다. 큐레이션한 266개는 "동맹이다" 라는 **편집 판단**이고,
공동발의 125개는 "119대에서 78건을 함께 발의했다" 는 **측정값**이다. 같은 선으로
그리면 보는 사람이 둘을 구분할 수 없고, 그러면 검증 가능한 쪽까지 신뢰를 잃는다.
그래서 `cosponsor` 는 별도 타입이고 범례에서 따로 끈다.

**공동발의는 방향이 있다.** B 가 A 의 법안에 서명한 것이지 그 반대가 아니다. 쌍으로
합칠 때 방향을 버리면 15/0(일방적 지지)과 15/15(상호)가 같은 선이 된다 — 125쌍 중
25쌍이 80% 이상 일방적이다. 65% 이상 기울면 `initiator` 에 **서명한 쪽**을 넣고,
그 미만은 상호적이라고 보고 방향을 붙이지 않는다.

`initiator` 는 원래 feud 전용이었는데 도메인에서 일반화했다. 뜻은 둘 다 "여기서
무언가가 상대에게로 간다" 이다. a·b 를 id 순으로 뒤집을 때 방향 집계를 같이 뒤집지
않으면 **화살표가 정확히 반대**를 가리키는데 눈으로는 그럴듯해서 못 잡는다.
`audit:data` 의 `cosponsor.direction` 이 이걸 본다.

**"같은 당" 은 정당이 아니라 코커스로 본다.** Sanders·King 은 무소속이지만 민주당과
코커스한다. 정당 문자열만 비교하면 Markey × Sanders 가 "초당적" 이 되어 초당적 쌍이
11개에서 19개로 부푼다. 크로스워크의 `caucus` 필드를 쓴다.

원시 건수를 그대로 쓰면 안 되는 이유도 있다. 10건 이상 쌍의 84% 가 같은 당이다.
표결 일치도(Cruz × Hawley 98.6%)가 관계가 아니라 소속이었던 것과 같은 함정이다.
기준선을 10건에 둔 것은 5건이면 신규 264개가 되어 큐레이션한 266개가 묻히기 때문이다.

이미 큐레이션된 쌍은 `duplicate` 로 표시해 그래프에서 뺀다 — 두 노드 사이에 선을
두 번 그으면 읽을 수 없다. `audit:data` 가 이 표시가 실제와 맞는지 검사한다.

### 매칭 실패를 확정으로 취급하지 않는다

크로스워크에서 "후보를 못 찾음" 을 자동 확정으로 처리했더니 Mike Lawler·Dick Durbin·
Chris Murphy 등 **7명이 조용히 빠졌다.** 데이터에는 Michael·Richard·Christopher 로
들어있고 별칭 필드는 비어 있다. 실패가 아니라 "해당 없음" 으로 끝나서 아무도 몰랐다.

통칭 사전(Mike→Michael)을 코드에 넣는 것은 답이 아니다 — 그 사전이 곧 새로운
흔들림의 원인이 된다. **성이 같은데 못 맞춘 경우를 사람에게 올리고**, 확정값을
`crosswalk-overrides.json` 에 적는다. 적지 않으면 쓰지 않고 종료 코드 1 로 끝난다.
같은 이유로 `Number('')` 는 `0` 이다 — 빈 icpsr 를 그냥 넘기면 0번 의원이 생긴다.

### 파괴적 스크립트는 `--dry` 먼저

`scripts/sources/*` 와 파이프라인은 데이터를 덮어쓴다. 제거율을 먼저 보고
반영한다. 실제로 필터가 802 → 381(52% 제거)이었고, 이 수치를 보지 않고
반영했으면 되돌리기 어려웠다.

플래그 방향은 하나다. **`--dry` = 쓰지 않는다.** 부작용을 "켜는" 플래그를
새로 만들지 않는다 — 어느 쪽이 안전한지 매번 확인하게 된다.
`--fetch`(네트워크 단계)와 `--links`(느린 링크 검사)는 쓰기와 다른 축이라 예외로
등록돼 있다. 새 플래그가 필요하면 `scripts/audit/conventions.mts` 의 목록에 먼저
추가한다. `audit:conventions` 가 이 관례를 강제한다.

### LLM 배치에서 침묵을 삭제로 해석하지 않는다

판정을 못 받은 항목은 **원본을 유지**한다. dry-run 중 503 이 2회 났고,
침묵을 "제거" 로 처리했다면 그 배치의 근거가 통째로 날아갔다.

### `generatedAt` 은 재처리가 갱신하면 안 된다

"뉴스를 수집한 시각" 이지 "파일을 다시 쓴 시각" 이 아니다. 분할·정규화가
이 값을 덮어써서 화면 배지가 27.7시간 어긋난 적이 있다.
`writeOutput` 의 기본은 보존이고, 실제 수집만 `{ fresh: true }` 를 준다.

### 출력 구조를 바꾸면 워크플로가 커밋하는 경로도 바꾼다

월별 분할로 `news-signals.json` 을 지웠는데 `daily-news.yml` 이 그 경로를
계속 스테이징하고 있었다. `git add` 는 없는 경로에서 실패하므로, 수집은
성공하고 **커밋 단계에서 죽어 그날 누적분이 통째로 유실될** 뻔했다.

### 근거 링크는 확인 가능한 것만

Google News 리다이렉트(`news.google.com/rss/articles/...`)는 목적지도 매체도
확인할 수 없다. 근거 패널에 넣지 않는다 — `audit:data` 가 막는다.
뉴스 신호(Latest Wire)는 별개다. 거기서는 "최근 이런 보도가 있었다" 용도라
기준이 다르다.

## 구조

```
src/domain/                      순수. 값 import 금지 (audit:boundary 가 강제)
scripts/news-pipeline/core.mts   누적·선별·파티션 규칙 (순수)
scripts/audit/*-rules.mts        감사 판정 규칙 (순수)

src/lib/, scripts/**/*.mts       어댑터. 데이터셋·fs·network 를 여기서 묶는다
src/data/signals/                월별 파티션 + 매니페스트 + recent
src/data/crosswalk.json          소스 간 ID 확정값 (bioguide·icpsr·fec)
src/data/cosponsorship*.json     공동발의 엣지 + 근거 (엣지는 즉시, 근거는 지연)
src/data/funding.json            FEC 자금 (지연 로딩, 인물 속성)
```

도메인에 값 import 를 넣고 싶어지면 그건 인자로 받아야 할 것이다.
이유는 ADR 0001.

## 데이터

- 저장소가 곧 DB 다. 야간 커밋 하나하나가 스냅샷이고, 실제로 데이터가 잘렸을 때
  git 이력으로 복구했다. DB 를 넣을 조건은 ADR 0002 에 적어 뒀다
- 아카이브는 365일 누적. **덮어쓰지 않는다** — 신규가 비어도 기존은 남아야 한다
- 문서(README)의 수치는 손으로 갱신해 왔고 조용히 낡는다. `audit:data` 가
  실제 데이터와 대조한다

## 하네스: POLARIS

**목표:** 관계 그래프·수집 파이프라인·근거·화면·문서에 걸친 작업을 영역별
전문 에이전트로 나누고, 각 단계를 실행 가능한 검증으로 판정한다.

**트리거:** 여러 영역에 걸치거나 범위가 넓은 요청은 `polaris` 스킬을 사용하라.
한 파일 수정이나 단순 질문은 직접 처리해도 된다.

에이전트는 `.claude/agents/`, 스킬은 `.claude/skills/` 에 있다.
구축 전에 무엇이 필요했는지와 그 근거는 ADR 0003.

**변경 이력:**

| 날짜 | 변경 내용 | 대상 | 사유 |
| --- | --- | --- | --- |
| 2026-08-29 | 초기 구성 (에이전트 5, 스킬 3) | 전체 | ADR 0003 의 선행 조건 1~4 완료 후 |
